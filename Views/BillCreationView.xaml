<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="RepairShopBilling.Views.BillCreationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource DarkGreyBackgroundBrush}">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Assets/Styles/AppStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock 
                Grid.Row="0"
                Text="Create New Bill" 
                Foreground="{StaticResource WhiteTextBrush}"
                FontSize="24"
                FontWeight="Bold"
                Margin="0,0,0,20"
                HorizontalAlignment="Left" />

            <!-- Customer Information Section -->
            <Border Grid.Row="1" Style="{StaticResource BillCard}" Margin="0,0,0,20">
                <StackPanel Spacing="16">
                    <TextBlock 
                        Text="Customer Information" 
                        Foreground="{StaticResource WhiteTextBrush}"
                        FontSize="18"
                        FontWeight="SemiBold" />
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Spacing="8">
                            <TextBlock 
                                Text="Customer Name" 
                                Foreground="{StaticResource LightGreyTextBrush}"
                                FontSize="14" />
                            <TextBox 
                                x:Name="CustomerNameTextBox"
                                Style="{StaticResource RoundedTextBox}"
                                PlaceholderText="Enter customer name"
                                Text="{x:Bind ViewModel.CustomerName, Mode=TwoWay}" />
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2" Spacing="8">
                            <TextBlock 
                                Text="Device Type" 
                                Foreground="{StaticResource LightGreyTextBrush}"
                                FontSize="14" />
                            <TextBox 
                                x:Name="DeviceTypeTextBox"
                                Style="{StaticResource RoundedTextBox}"
                                PlaceholderText="e.g., Laptop, Desktop, Phone"
                                Text="{x:Bind ViewModel.DeviceType, Mode=TwoWay}" />
                        </StackPanel>
                        
                        <StackPanel Grid.Column="4" Spacing="8">
                            <TextBlock 
                                Text="Date" 
                                Foreground="{StaticResource LightGreyTextBrush}"
                                FontSize="14" />
                            <DatePicker 
                                x:Name="BillDatePicker"
                                Style="{StaticResource RoundedDatePicker}"
                                Date="{x:Bind ViewModel.BillDate, Mode=TwoWay}" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Service Categories Section -->
            <Border Grid.Row="2" Style="{StaticResource BillCard}" Margin="0,0,0,20">
                <StackPanel Spacing="16">
                    <TextBlock 
                        Text="Service Categories" 
                        Foreground="{StaticResource WhiteTextBrush}"
                        FontSize="18"
                        FontWeight="SemiBold" />
                    
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        
                        <!-- First Row of Categories -->
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            
                            <Button Grid.Column="0" Style="{StaticResource RedAccentButton}" 
                                    Content="HOURS" Margin="0,0,6,0"
                                    Click="OnServiceCategoryClick" Tag="HOURS" />
                            <Button Grid.Column="1" Style="{StaticResource RedAccentButton}" 
                                    Content="EQUIPMENT" Margin="6,0,6,0"
                                    Click="OnServiceCategoryClick" Tag="EQUIPMENT" />
                            <Button Grid.Column="2" Style="{StaticResource RedAccentButton}" 
                                    Content="WIN 11 PRO" Margin="6,0,6,0"
                                    Click="OnServiceCategoryClick" Tag="WIN 11 PRO" />
                            <Button Grid.Column="3" Style="{StaticResource RedAccentButton}" 
                                    Content="OS X" Margin="6,0,6,0"
                                    Click="OnServiceCategoryClick" Tag="OS X" />
                            <Button Grid.Column="4" Style="{StaticResource RedAccentButton}" 
                                    Content="DRIVERS" Margin="6,0,0,0"
                                    Click="OnServiceCategoryClick" Tag="DRIVERS" />
                        </Grid>
                        
                        <!-- Second Row of Categories -->
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            
                            <Button Grid.Column="0" Style="{StaticResource RedAccentButton}" 
                                    Content="VHS CONVERT" Margin="0,0,6,0"
                                    Click="OnServiceCategoryClick" Tag="VHS CONVERT" />
                            <Button Grid.Column="1" Style="{StaticResource RedAccentButton}" 
                                    Content="DATA REC." Margin="6,0,6,0"
                                    Click="OnServiceCategoryClick" Tag="DATA REC." />
                            <Button Grid.Column="2" Style="{StaticResource RedAccentButton}" 
                                    Content="PHOTO PRINT" Margin="6,0,6,0"
                                    Click="OnServiceCategoryClick" Tag="PHOTO PRINT" />
                            <Button Grid.Column="3" Style="{StaticResource RedAccentButton}" 
                                    Content="LASER" Margin="6,0,6,0"
                                    Click="OnServiceCategoryClick" Tag="LASER" />
                            <Button Grid.Column="4" Style="{StaticResource RedAccentButton}" 
                                    Content="CUSTOM" Margin="6,0,0,0"
                                    Click="OnServiceCategoryClick" Tag="CUSTOM" />
                        </Grid>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Current Bill Section -->
            <Border Grid.Row="3" Style="{StaticResource BillCard}">
                <StackPanel Spacing="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock 
                            Grid.Column="0"
                            Text="Current Bill" 
                            Foreground="{StaticResource WhiteTextBrush}"
                            FontSize="18"
                            FontWeight="SemiBold" />
                        
                        <TextBlock 
                            Grid.Column="1"
                            Text="{x:Bind ViewModel.TotalAmount, Mode=OneWay}"
                            Foreground="{StaticResource RedAccentBrush}"
                            FontSize="18"
                            FontWeight="Bold" />
                    </Grid>
                    
                    <!-- Bill Items List -->
                    <ListView 
                        x:Name="BillItemsListView"
                        ItemsSource="{x:Bind ViewModel.BillItems, Mode=OneWay}"
                        Background="Transparent"
                        BorderThickness="0"
                        SelectionMode="None"
                        MaxHeight="300">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="0,8" Background="Transparent">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock 
                                        Grid.Column="0"
                                        Text="{Binding Description}"
                                        Foreground="{StaticResource WhiteTextBrush}"
                                        FontSize="14"
                                        VerticalAlignment="Center" />
                                    
                                    <TextBlock 
                                        Grid.Column="1"
                                        Text="{Binding Quantity}"
                                        Foreground="{StaticResource LightGreyTextBrush}"
                                        FontSize="14"
                                        Margin="16,0"
                                        VerticalAlignment="Center" />
                                    
                                    <TextBlock 
                                        Grid.Column="2"
                                        Text="{Binding TotalPrice, Converter={StaticResource CurrencyConverter}}"
                                        Foreground="{StaticResource RedAccentBrush}"
                                        FontSize="14"
                                        FontWeight="Bold"
                                        Margin="16,0"
                                        VerticalAlignment="Center" />
                                    
                                    <Button 
                                        Grid.Column="3"
                                        Content="✕"
                                        Background="Transparent"
                                        Foreground="{StaticResource LightGreyTextBrush}"
                                        BorderThickness="0"
                                        FontSize="12"
                                        Padding="8,4"
                                        Margin="8,0,0,0"
                                        VerticalAlignment="Center"
                                        Click="OnRemoveItemClick"
                                        Tag="{Binding}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    
                    <!-- Action Buttons -->
                    <Grid Margin="0,16,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        
                        <Button 
                            Grid.Column="0"
                            Style="{StaticResource RedAccentButton}"
                            Content="Save Bill"
                            Command="{x:Bind ViewModel.SaveBillCommand}"
                            IsEnabled="{x:Bind ViewModel.CanSaveBill, Mode=OneWay}" />
                        
                        <Button 
                            Grid.Column="2"
                            Style="{StaticResource RedAccentButton}"
                            Content="Preview Bill"
                            Command="{x:Bind ViewModel.PreviewBillCommand}"
                            IsEnabled="{x:Bind ViewModel.CanPreviewBill, Mode=OneWay}" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>